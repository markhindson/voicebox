# ARM64 Windows requirements — excludes packages without win_arm64 wheels
# Differences from requirements.txt:
#   - torchaudio: no arm64 wheels
#   - numba: depends on numpy<2.1, no arm64 wheels
#   - pedalboard: JUCE C++ extensions, no arm64 wheels
#   - numpy: uses >=2.0 (arm64 wheels available), not <2.0
# torch is pre-installed from the CPU index before this file runs

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# ML models
transformers>=4.36.0,<=4.57.6
accelerate>=0.26.0
huggingface_hub>=0.20.0

# LuxTTS
--find-links https://k2-fsa.github.io/icefall/piper_phonemize.html
linacodec @ git+https://github.com/ysharma3501/LinaCodec.git
Zipvoice @ git+https://github.com/ysharma3501/LuxTTS.git

# Chatterbox TTS sub-dependencies
conformer>=0.3.2
diffusers>=0.29.0
omegaconf
pykakasi
resemble-perth>=1.0.1
s3tokenizer
spacy-pkuseg
pyloudnorm

# Kokoro TTS
kokoro>=0.9.4
misaki[en,ja,zh]>=0.9.4
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
unidic-lite>=1.0.8

# Audio processing (no numba — librosa falls back to pure-Python)
librosa>=0.10.0
soundfile>=0.12.0

# HTTP client
httpx>=0.27.0

# MCP server
fastmcp>=3.0,<4.0
sse-starlette>=2.0

# Utilities
python-multipart>=0.0.6
Pillow>=10.0.0
